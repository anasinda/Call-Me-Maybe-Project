[project]
name = "call-me-maybe"
version = "1.0.0"
description = "Implementing constrained decoding on a small LLM"
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    "pydantic",
    "transformers"
]
