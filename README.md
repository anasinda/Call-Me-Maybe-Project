# Call-Me-Maybe-Project
A Python project that explores LLM function calling using the Qwen3-0.6B model. It converts natural language prompts into structured, schema-compliant JSON function calls through constrained decoding, ensuring reliable, machine-readable output instead of free-form text generation.
